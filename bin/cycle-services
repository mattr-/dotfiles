#!/bin/sh
for session in `tmux ls -F "#{session_name}"`
do
    echo "Cycling session $session"
    tmux send-keys -t "${session}:2" C-c 'bundle && STEPFORD_LOCAL=1 bundle exec foreman start' Enter
done
