#!/bin/sh
set -e

function die() {
  echo "$@"
  exit 1
}
[[ $1 == "" ]] && die "Not enough arguments"

pushd ~/Code/jekyll/jekyll
git pull --rebase
vim History.markdown
git merge-pull $1
update-jekyll-history $1
popd


