# Do nothing inside of a codespace
[[ -n $CODESPACES ]] && exit 0

# Assume `sudo` is configured correctly for our user
sudo apt-get update

sudo apt-get install -y --no-install-recommends \
 zsh \
 tmux \
 jq \
 ripgrep \
 myrepos \
 fzf \
 hub